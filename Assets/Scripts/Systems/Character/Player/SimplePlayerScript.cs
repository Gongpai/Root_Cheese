namespace GDD
{
    public class SimplePlayerScript : HealthSystem
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}